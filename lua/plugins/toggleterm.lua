-- [[ Configre Toggleterm ]]

require('toggleterm').setup({
  size = 20,
  open_mapping = [[<C-ä>]],
  hide_numbers = true,
  shade_terminals = true,
  shading_factor = 0.5,
  direction = "horizontal"
})
